#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
ll n;
int main() {
    cin.tie(0)->sync_with_stdio(0);
    cin >> n;
    if(n % 2) cout << "SK" << '\n';
    else cout << "CY" << '\n';
}